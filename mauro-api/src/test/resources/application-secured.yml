micronaut:
  security:
    enabled: true

    authentication-provider-strategy: any
    authentication: idtoken

    redirect:
      enabled: true
    endpoints:
      login:
        enabled: true
        path: '/authentication/login'
      logout:
        enabled: true
        #        path: '/authentication/logout'
        get-allowed: true
      cookie:
        enabled: true
      oauth:
        enabled: true
      session:
        enabled: true
      reject-not-found: false
    oauth2:
      clients:
        keycloak:
          client-id: ${OAUTH_CLIENT_ID:sandbox}
          #          replace client secret
          client-secret: ${OAUTH_CLIENT_SECRET:hzZmVQ4UDDIwrORODOzJ3JBPxZX5jh7y}
          openid.issuer: ${OIDC_ISSUER_DOMAIN:`http://localhost:`${random.port}}/realms/${KEYCLOAK_REALM:master}

